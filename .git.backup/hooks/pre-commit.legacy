#!/bin/bash
echo "Running pre-commit hook ($0)..."

#if rg -H "(debugger|breakpoint)" `git diff --cached --name-only --diff-filter=ACM`; then
#    echo "Found debug statement"
#    exit 1
#fi

exit 0
